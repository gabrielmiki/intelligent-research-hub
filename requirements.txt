# --- Core Django Framework ---
# Using the latest stable release as of Jan 2026
Django==6.0.1
djangorestframework==3.15.2
django-cors-headers==4.4.0  # REQUIRED: To allow your Frontend to talk to this API

# --- Application Server (Production) ---
# Uvicorn for ASGI (Async) + Gunicorn for Process Management
uvicorn[standard]==0.32.0
gunicorn==23.0.0

# --- Database (PostgreSQL) ---
# psycopg2-binary is fine for containers, but 'psycopg' (v3) is the modern standard.
# Keeping binary for simplicity in your Docker setup.
psycopg2-binary==2.9.9

# --- Async Task Queue (The Engine) ---
celery==5.5.0
redis==5.1.0
flower==2.0.1  # REQUIRED: To visualize/monitor your AI tasks in a dashboard

# --- Configuration & Security ---
django-environ==0.11.2  # BETTER: Handles DATABASE_URL parsing automatically

# --- API Documentation ---
drf-spectacular==0.28.0 # Generates your Swagger/OpenAPI UI

# --- AI & Research (Google Vertex AI) ---
# The core SDK to talk to Vertex AI Models and Search
google-cloud-aiplatform==1.42.0 
# (Optional) If you are building code-first agents using Google's framework:
google-adk==0.5.2 

# --- Utilities ---
requests==2.32.0   # For simple HTTP calls inside your Researcher logic
beautifulsoup4==4.13.0 # For cleaning HTML content from websites